# 阶段 0 协议样本

`stage0-sanitized.jsonl` 来自 2026-09-19 本地自有账号联调，仅保留字段结构。
字符串和数字值均为每次会话随机盐摘要；字段类型保留在 `<str:...>`、`<int:...>` 前缀中。
这些结构样本不含真实 UID、正文、Cookie、Token，不能直接重放为有效协议，也不能代替业务验收。

实时与历史字段语义的交叉核对证据见 `docs/bridge-development.md`。自动测试中的协议事件使用人工合成值，需与真实样本区分。
