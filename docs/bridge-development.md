# 消息桥开发记录

## 根目录单一 Skill（2026-09-20）

用户确认整个仓库本身就是 Skill。根 SKILL.md、src、scripts 和 docs 构成完整交付，不再维护内层 Skill 及业务源码副本；详细方案见 [ROOT_SKILL_PLAN.md](../ROOT_SKILL_PLAN.md)。旧嵌套封装已完整备份至仓库外，原 src、账号、配置、环境和正在运行的服务保持原状。

根管理入口现在可直接查询、检查和管理原项目，不要求另建实例或改写实例身份。独立新部署只生成一份 runtime，旧 management 快捷方式通过兼容入口转发；发布导出仅复制根清单的公开资源。下面的标准封装章节为此前 0.1.0 历史记录，当前结构与验收以新方案及 skill-validation.md 为准。

## 标准 Skill 封装（2026-09-20）

新增自包含 xianyu-feishu-bridge Skill 及独立实例管理，保留现有消息业务。已完成资源哈希、隔离 NPX 安装、实例初始化、Windows 模拟启停、升级与备份验证；完整证据及发布待办见 [skill-validation.md](skill-validation.md)。当前正式实例未迁移、未重启，新增心跳要在未来明确切换后才会用于该实例。

## 比特分组自动创建本地账号（2026-09-19）

- 按用户要求，将固定三个手工账号槽位改为启动时发现比特闲鱼分组，按 UID 对比配置和磁盘绑定、去重并自动补建。已有编号和消息路由保持稳定，新增账号保存独立加密凭据与窗口 ID，发现元数据单独持久化；不注册新的闲鱼账号。
- 回归覆盖五个窗口建号、已有三账号补建两个、重复执行、同 UID 多窗口、同名不同 UID、窗口身份冲突、空分组、未登录跳过、分页与启动时默认账号选择。先验证失败，再实现通过。
- 当前只在启动或 `sync-accounts` 时同步，常驻进程不热添加账号。2026-09-19 23:49 按用户要求安全重启现有服务，启动同步成功，A1、A2、A3 均重新进入 ONLINE；真实新增窗口建号尚未验证，相关场景已通过离线回归。日志仍有既有历史补拉不完整提示。
- 验证完成：全量 366 项测试通过，全仓 Ruff 与 `git diff --check` 通过，命令帮助中文输出正常；仅既有飞书 SDK datetime 弃用提示。

## 高频问题记录

- 2026-09-20：后台启动不得弹出 CMD，不能仅凭启动参数或窗口停留在提示符就断言关闭不影响服务；必须验证实际主进程及子进程控制台归属，并在启动器退出后复查心跳。用户反馈飞书无响应时先查真实进程，数据库 ONLINE 不能代替存活证据。
- 2026-09-20：用户再次指出已登录比特窗口被漏判。Cookie 接口为空、过期或读取异常均不能直接认定浏览器退出登录；须核对原绑定窗口的实时上下文，并区分凭据获取失败与服务端认证失败。
- 2026-09-20：用户明确要求整个项目仓库本身就是一个完整 Skill：唯一 SKILL.md 放仓库根目录，直接使用现有 src/、scripts/ 和项目依赖文件，不再嵌套 skills/xianyu-feishu-bridge/ 或复制一份业务源码到 assets/runtime/。后续结构调整以此为准，沿用现有业务逻辑和正式实例数据；此前嵌套发布包结构仅为历史实现，不能继续作为目标方案。
- 2026-09-20：用户询问本项目 Skill 实例与原实例时，应先结合当前工作目录和项目方案、架构、最新验收记录确定指代，不反复要求用户提供已有名称或路径；解释时区分 Skill 资源包、正式运行实例和隔离验收实例，并区分文档记录状态与实时状态。
- 2026-09-19：用户明确以比特窗口作为账号代理配置源。启用比特来源时直接读取对应窗口，不能要求另填代理环境变量维护两份配置；需同时覆盖 HTTP、HTTPS、SOCKS5、认证及代理端 DNS，并在配置读取失败时停止，禁止静默直连。

## 比特窗口代理来源与真实 SOCKS5 验证（2026-09-19）

- 已读取用户指定“北美草原狼”窗口：自定义 SOCKS5，用户名和密码均已配置，窗口声明为 Windows / Chrome 147.0.7727.57。代理地址、认证信息及 Cookie 未输出到报告或日志。
- 启用比特来源时，同窗口提供 Cookie、代理及 UA，保留分组、名称唯一匹配及 Cookie UID 与本地绑定校验；手工 network/client 不再参与，只有明确 noproxy 才直连。Cookie 刷新移至 load_session，一次完成，避免 Worker 启动两次初始化导致重复读取或打开窗口。配置在账号会话创建时固定，重连沿用，不自动切换出口。
- 支持 HTTP、HTTPS、SOCKS5，SOCKS5 使用代理端 DNS。仅新增 PySocks 1.7.1、python-socks 3.1.1，未同步移除旧环境依赖；锁文件已更新。动态 API 提取、全局代理、SSH 未实现可靠解析，明确拒绝；带认证 SOCKS5 应在原比特窗口登录，独立扫码 Chrome 入口明确拒绝。
- 离线真实 SOCKS5 服务测试发现 websockets 15 将 URL 编码后的认证字符串直接发送，特殊字符密码与 requests 行为不同。已用独立连接子类对认证字段解码，保留标准 HTTP/HTTPS CONNECT、目标 WSS TLS 和证书校验，不修改第三方模块全局函数。依赖限定 websockets 15.x；本机真实 TLS 隧道测试同时覆盖 HTTP 与 HTTPS 代理中的特殊字符认证。
- 首轮 ipify、闲鱼首页 HTTPS 出现 SSLEOFError，部分其他目标连接失败；随后对照及最终代码复测均为闲鱼首页 200、API 域名根路径 404（证明 TLS/HTTP 可达，不代表业务接口成功）、HTTPS 对照站点 200、闲鱼 WSS 握手成功。WSS 未携带 Cookie、Token 或发送 /reg，不是账号登录、收件或长期稳定性验收。保留初期连接失败记录，不宣称代理长期稳定或出口 IP 已对照。
- 最终全量 356 项测试、全仓 Ruff、离线锁文件检查、git diff --check、本地 doctor 通过；仅既有飞书 SDK datetime 弃用提示。现有常驻服务未重启，运行中的旧进程尚未切换代理；未修改实际 config.yaml、.env、浏览器窗口设置或真实账号凭据。新逻辑下次重启时生效。
- 匿名复测入口：`python scripts/probe_bitbrowser_proxy.py --account A1`。字段依据：[比特官方窗口 API](https://doc.bitbrowser.net/api-docs/browser-profiles)；SOCKS 依赖和代理行为参考：[websockets 代理文档](https://websockets.readthedocs.io/en/stable/topics/proxies.html)，实际兼容性以本地锁定 15.0.1 源码和测试为准。

## 账号代理与客户端一致性修复（2026-09-19）

- 按用户确认，先实现代理能力，现有本地配置继续直连；不修改真实 `.env`、Cookie、设备缓存或运行服务。每账号可通过独立环境变量配置 HTTP/HTTPS 代理；不支持 SOCKS，飞书保持直连。
- NetworkProfile 创建时固定代理地址，HTTP、WebSocket、消息收发、历史查询及重连共用账号出口。独立扫码 Chrome 使用同一账号代理，保留浏览器自身 UA；比特窗口仍由比特管理其网络与设备配置，不自动继承或改写。
- 新增 ClientProfile，将协议 HTTP UA、Client Hints、WebSocket UA 和 IM 注册声明统一到同一账号配置。默认沿用长连接的 Windows 10 / Chrome 133，可通过 client.user_agent 显式配置其他受支持的桌面 Chrome UA；同步声明不等于复制浏览器指纹。
- 设备 ID 改为从账号明确路径读取，保留已有值；代理、UA 和同账号 Cookie 更新不重置设备 ID，不清空 IM Token。独立会话持有连接工厂，不再在桥接长连接入口无条件创建默认直连出口。
- 先通过失败测试复现代理未实现、长连接丢失账号代理、HTTP/WS 声明不一致，以及扫码入口未传网络配置，再逐项修复。测试覆盖缺失/无效代理拒绝、账号隔离、重连固定出口、已有设备缓存保留、自定义 UA 和飞书不支持代理时明确拒绝。
- 本机真实代理协议测试验证 requests 的 HTTP 请求、HTTPS CONNECT 和 websockets 的 WSS CONNECT，代理拒绝后抛错，不回退直连；未使用真实外部代理，也未向闲鱼发送测试消息。
- 验证：全量 337 项测试通过（仅既有飞书 SDK datetime 弃用提示）；最后扫码参数调整后相关 15 项复核通过。Ruff、git diff --check、离线锁文件检查、本地 doctor 通过，中文输出正常。websockets 下限修正为 15，锁定版本仍为 15.0.1，没有升级安装依赖。配置方法见启动说明；新代码待下次正常重启加载，外部代理及真实平台联调尚未执行。

设计依据：仓库内 `docs/goofish_feishu_bridge_plan.md`（工作区原始方案的归档副本），2026-09-19 v1.0。

## 实施顺序与边界

1. 环境与目录：在 `goofish-feishu-bridge` 克隆上游，使用项目 `.venv`。
2. 阶段 0：开发单账号隔离登录、直连、监听/有界历史/发送验证工具，以及飞书引用事件探针。
3. 验证：自动测试只证明本地行为；真实协议与手机引用事件通过后，才实施阶段 1～4 的常驻消息桥。

上游固定 commit：`469cac533759f5fc3f5e3314e63b344d1dc454a5`。
Python：3.12.14。依赖以 `uv.lock` 为准；不自动升级。

## 阶段 0 设计

- 一个命令进程只能装载一个账号，启动后禁止切换账号。目录预留 A1/A2/A3，不启动三账号服务。
- `AccountPaths` 管理全部 Cookie、设备、IM Token、限流、风控和扫码浏览器目录。
- 适配层仅在账号进程初始化时重定向上游缓存常量；Cookie 默认从比特浏览器“闲鱼”分组按账号昵称读取，优先读取已打开窗口的实时 Cookie；实时为空时先检查窗口状态，已打开则报错等待页面加载，不关闭用户窗口。已关闭时才读取详情接口同步快照，快照为空、关键字段不完整或 H5 Token 剩余不足一分钟时临时无头打开原账号，加载闲鱼首页等待有效 Cookie；成功或失败均关闭临时窗口并检查进程退出。分组、昵称或 UID 校验失败时停止该账号，不自动选择其他浏览器身份。
- HTTP 使用 `trust_env=False`，WebSocket 显式 `proxy=None`，扫码 Chrome 使用 `--no-proxy-server`。
- 登录后在本地显示 Cookie 声明的 UID，人工输入相同 UID 确认绑定；服务端身份仍需探针联调核验。
- 监听保留全部推送结构，接收注册成功回包及 `/s/vulcan` 后才报告就绪。
- 采样只保存脱敏结构，字符串使用带随机盐的摘要；不猜测正常消息的源消息 ID 与源时间路径。
- 发送只用于明确指定的自有测试客户与已有会话，发出前持久化请求；发出后无明确回包记为 UNKNOWN，不重试。
- 飞书探针只绑定明确确认的本人私聊，保存 `message_id` 与 `parent_id`，不触发闲鱼发送。

## 当前验收状态

2026-09-19：

- 已创建项目与 `.venv`，锁定 Python 3.12.14、上游 commit 和 `uv.lock`。
- A1/A2/A3 均已扫码、人工确认不同 UID 并保存独立凭据，目录之间不共享可变缓存。
- A1 获得真实 `/reg` 成功回包与 `/s/vulcan`，首轮观察收到 21 帧、解析 14 条消息事件；包含连接补发，不能全部计作新测试消息。
- 使用测试客户随机文字标记，精确确认了账号、客户 UID 和已有会话 CID。
- 有界历史查询返回 3 条消息，正常结束。在同一条测试消息上确认：实时 `1.3` 与历史 `message.messageId` 完整相等，实时 `1.5` 与历史 `message.createAt` 完整相等；`1.1.1` 实际是带后缀的发送者 UID，不是消息 ID。
- 飞书本人私聊已人工绑定；实际发送一条提示，手机直接引用事件的 `parent_id` 与提示 `message_id` 完全一致。
- 已向明确授权的自有测试会话发送一条测试回复，状态为 `SERVER_ACCEPTED`，用户确认客户成功收到。
- 另开发送连接后原监听断开，双连接方案未通过验证；已改成同连接收发、唯一接收协程按 mid 分配回包，并通过回归测试。再次真实发送已被服务端接受，随后继续收到测试客户消息，`received_after_send=true`。
- 普通单条消息身份已有样本交叉验证；轻量通知补正文、离线新会话发现、更多消息类型尚未验证。

阶段 0 关键门槛通过后，已实现并测试阶段 1～3 的消息桥代码。当前交付为本地 MVP 开发版，尚未通过可带出门使用的完整验收。

## 真实双向闭环结果

- 使用 A1 与本人控制的测试客户运行受限模式 300 秒，其他客户被过滤，业务数据写入独立 `bridge-test.sqlite`。
- 测试客户新消息 → 飞书客户转发成功；本人直接引用 → 生成唯一回复任务 → 闲鱼服务端接受 → 飞书队列/结果回执成功。
- 用户确认测试客户收到回复。停止后数据库为：客户转发 1 条、回复任务 1 条 `SERVER_ACCEPTED`、异常回复 0、异常飞书投递 0。
- A1 运行期间另起 A2、A3 独立探针，均通过真实注册与接收验证；A2 观察 6 帧/17 条消息事件，A3 观察 7 帧/8 条消息事件。包含连接补发，不能算作各账号 5 条新消息验收。
- 三账号登录、A1 双向闭环与收发共存属于真实测试；跨账号固定路由、事件去重、过期、崩溃恢复、错误引用、非本人拒绝、磁盘异常前不 ACK 等属于自动模拟测试。
- 受限测试已按时退出。未开启不受限的后台常驻，未自动注册计划任务。

## 尚未通过的验收项

- A2、A3 各自真实飞书引用回复；每账号至少 5 条新文本/3 次回复；同测试客户跨账号往返。
- 真实 `new_msg` 轻量通知补正文（代码已实现，当前采样未观察到该事件）；图片和未知类型提示的真实样本覆盖。
- 断网 1～5 分钟的恢复、离线期间新会话覆盖、长时间凭据有效性、锁屏和计划任务运行身份。
- 三账号 24 小时持续运行、性能时延统计，以及系统 TUN/透明路由的实际直连出口验收。
- 有限会话列表和历史接口仍受平台能力限制；未补齐会话写入 `sync_state.gap` 并发告警，不声称任意离线都能完整恢复。

## 排障与回归记录

- 上游 IM 请求 `mid`、客户端 UUID 有协议格式；替换为普通 UUID 时，实际历史查询不返回匹配回包。恢复上游生成函数后查询成功。已添加格式回归检查，正式适配层沿用同一生成方式。
- 飞书 SDK 正常断开时，必须先取消并回收 SDK 接收协程，再关闭 WebSocket；已用先失败再通过的回归测试覆盖。
- Windows 默认 GBK 下上游元数据测试读取失败，已显式指定 UTF-8。Unix 的 `st_mode == 0600` 不代表 Windows ACL，相关断言只在非 Windows 执行；当前运行目录另行实测设置 ACL。
- 所有账号缓存重定向仅在进程初始化时执行，不使用同进程切换账号；故障与未知发送不会自动重发。
- 样本字段名也可能包含 UID（例如 `squadId_<uid>`）；脱敏同时处理值和动态字段名，仅保留已知协议字段名。导出还会重新检查早期诊断记录，已补回归测试；私有诊断库仍按敏感数据保存在受限目录。
- 空会话的成功回包可能省略 `userMessageModels`，实测返回 `hasMore=0`、`degradeFailover=0`。只在这两个条件均成立时认定为空历史，其他未知结构仍报告缺口。初次运行产生的结构误报告警已修复；同账号补拉告警最多每分钟一条，所有具体缺口继续保存。
- 后来的实时消息只推进已见位置，不覆盖最后完整补拉检查点。数据库版本 2 自动新增 `sync_state.checkpoint`，升级不删除旧消息和任务。历史同步缺口只写本地 `sync_state` 和日志，不再推送到飞书；启动恢复会抑制旧版本遗留的历史告警队列。
- Windows PowerShell 5 脚本使用 UTF-8 BOM，已通过原生文件解析；只做语法验证，未执行计划任务注册。
- IM 注册 401/403 分别进入人工认证/风控暂停，避免把身份拒绝当作普通断线反复重连；发送接口出现对应拒绝时也暂停该账号。

## 阶段 1～3 实现拆分

1. 正式 `store` 与 `router`：五张业务表、固定映射、原子入站/出站、本人白名单、队列有效期、未知不重发。
2. `account_worker`：每账号一个 spawn 进程、复用单连接、事件经主进程落库确认、通知补正文和有界补拉。
3. `supervisor` 与飞书长连接：主进程唯一数据库写入通道、三账号状态监督、后台投递、结果回执、单实例和安全退出。
4. 配置检查、启动说明、离线故障测试和真实 A1 双向闭环；A2/A3 登录后验证隔离。

## 运行接口与持久化

`python -m goofish_bridge run` 默认启动三个已绑定账号；`--accounts A1` 用于逐账号联调；`--test-customer-uid` 开启指定客户白名单和独立测试数据库；`--duration` 限时运行。

`stop` 写入退出请求，主进程停止子进程并保存结果；退出后未确认的 `DISPATCHING` 保守转为 `UNKNOWN`。`status` 只读正式业务库，不启动网络连接。飞书回调与账号子进程均通过队列提交事件，业务数据库仅由主进程写入。

SQLite 初次由 `Store` 自动建表，按 `PRAGMA user_version` 执行非破坏性升级；不兼容的更高版本直接拒绝。真实数据、账号凭据、日志和个人配置均由 `.gitignore` 排除。

日志记录账号状态与错误类型，不记录正文或认证包；`logs/bridge.log` 每文件 2 MB、保留 3 份轮转备份。所有敏感运行目录已设置当前用户和 SYSTEM 的 Windows ACL。

飞书展示层使用账号中文展示名（A1“北美草原狼”、A2“AI编程哥”、A3“影刀RPA专家”）；客户卡片隐藏账号 UID、客户 UID、会话 ID 和内部任务字段，回复回执只显示正文与最终状态。内部数据库仍保留完整路由字段。

卡片回复模式已接入：`feishu_cards` 持久化“账号 + 客户 + 会话”映射，首次消息创建卡片，后续消息引用原卡片另发提醒，原卡片仅在用户主动“查看更多／收起”时刷新。默认显示最近 4 条，展开显示最近 12 条。卡片输入框动作按卡片 `message_id` 定位发送目标；直接引用原卡片和客户新消息提醒均可固定路由。飞书应用还需启用 `card.action.trigger` 事件，真实卡片动作闭环待在手机端验收。

## 最终本地验证

- 全量测试：315 项通过；1 条飞书 SDK 内部 datetime 弃用提示，不影响测试结果。
- 新增消息桥及改动测试文件的 Ruff 检查通过，`git diff --check` 通过。
- `uv build` 成功生成源码包和 wheel；wheel 包含 `goofish_bridge`，未包含账号目录或 `.env`。
- `doctor` 的依赖、虚拟环境、三个账号绑定及飞书环境变量检查全部通过。
- Windows 启停/计划任务脚本语法通过，未执行自启注册；敏感路径 Git 忽略及样本 UID 脱敏检查通过。
- 飞书返回成功但缺少消息 ID 时仍标为 `UNKNOWN`，不会错误宣称明确失败或自动重发。

## 私密仓库初始化

2026-09-19：保留上游 Git 历史，将消息桥开发成果纳入独立私密仓库。上游保留为 `upstream`，私密仓库使用 `origin`；原始方案归档到 `docs/goofish_feishu_bridge_plan.md`。账号凭据、环境配置、数据库、日志及虚拟环境继续由忽略规则排除。

## 卡片定位与草稿测试（链接复测成功，正式流程未接入）

2026-09-19：新增独立脚本 `scripts/probe-card-jump.py`，仅在明确执行时向已绑定本人私聊发送测试卡片与引用提醒。测试卡片禁用提交，不写入业务路由，不撤回或刷新真实客户卡片。每次运行的投递状态保存在被 Git 忽略的 `data/card-jump-probe-*.json`；未知投递不自动重试。

- 本次两次发送均由飞书返回成功及消息 ID；脚本 Ruff 通过，中文输出正常。
- 官方“打开聊天页面”协议仅列出 `openId` / `openChatId`；测试按钮附带的 `messageId` 是实验参数，不能据发送成功认定消息定位成功。
- 引用提醒同时提供飞书原生引用区域作为定位对照；用户已澄清该入口打开消息详情页，底部带独立输入框，并非直接定位原卡片输入框。详情页发送的路由关系尚未验证。
- 本轮不测试卡片刷新、撤回或接收新提醒瞬间对正在输入草稿的影响；这些需另行分步验证，不将本轮结果外推。
- 设计教训：提出“查看客户卡片”入口前，须先验证目标客户端的消息定位与草稿行为，不能将打开会话等同于定位消息。
- 第一轮用户反馈：点击按钮后草稿仍保留，但原卡片和提醒在同一屏，无法判断定位效果。仅记录“同屏点击后草稿保留”，不认定跨屏跳转通过。
- 第二轮：脚本新增 `--separate-from`，复用原测试卡片，发送含 30 行的分隔卡片及新的引用提醒；两次投递成功，原卡片未刷新或撤回。用户首次反馈链接未成功、引用可打开且草稿保留，随后再次测试确认链接跳转成功。首次与复测差异的原因未确认，不认定所有客户端均稳定通过。脚本 Ruff、中文帮助输出及 `git diff --check` 通过。
- 测试设计补充：定位测试的源消息与入口必须隔开到不同屏，明确以回到原卡片输入框为成功标准，避免同屏产生假阳性。

### 高频问题记录

- 2026-09-19：用户确认比特窗口已登录时，不能将桥接程序 AUTH_REQUIRED 直接解释成账号掉线并要求重新扫码；应先检查绑定窗口、实时 Cookie 读取结果与已同步快照的差异。Token 快照过期不等于浏览器登录失效。
- 2026-09-19：不得把“引用打开详情页”解释为“回到原卡片”，也不得凭一次失败断言链接不可用；分别记录入口、实际页面、草稿结果与复测结论。当前仅完成独立探针测试，尚未改动正式消息提醒和卡片刷新逻辑。
- 2026-09-19：用户明确要按客户详情页是否处于查看状态分流：页内收到新消息直接展示、不额外推主聊天，退出后才推主聊天；不能误解为始终推送。现有 SDK 仅见进入机器人单聊、消息已读等事件，未发现客户详情页进入／退出状态，不能用已读或最近回复时间代替。原生话题聚合与自建会话页是待评估方向，尚未执行改动。

## 客户新消息引用提醒（2026-09-19）

- 新增 outbox 类型 `CUSTOMER_REMINDER`，携带入站消息及卡片映射。投递时取得原卡片消息 ID，调用飞书 `message.reply` 发送文本，显式 `reply_in_thread=False`，正文为闲鱼账号展示名、客户昵称、新消息正文。每条提醒独立保存返回的消息 ID，引用提醒按持久化入站记录路由；不使用昵称猜测，不以 root_id 兜底。
- 首次卡片未确认成功时，后续提醒等待原卡片 ID，其他客户的投递不受阻。投递去重 UUID 使用 delivery_id。返回成功但缺消息 ID 或网络结果不明确，保持 UNKNOWN，不自动重发。
- 客户新消息只追加本地会话记录，不更新已有卡片；回复结果改为带账号及昵称的独立回执，避免结果晚到时覆盖下一条草稿。用户主动展开／收起才读取完整记录并刷新。
- 重启不再依赖 transcript 更新时间刷新卡片；旧版排队的入站卡片更新会在领取时转换为引用提醒，旧版非入站待刷新任务标记失败保留记录。仅明确 FAILED 的首次建卡可在恢复时重建，并保留原入站映射；UNKNOWN 建卡等待人工核对。
- 兼容旧 schema 重建卡片：直接引用当前卡片 ID 也从 `feishu_cards` 解析固定目标，保留账号 UID 校验。数据库字段兼容版本 3，无删除或结构升级。
- 已用先失败再通过的测试覆盖提醒显示、源消息去重、连续入站、同名客户跨账号路由、未知投递、重启草稿保护、首次建卡失败恢复、旧任务兼容和重建卡片引用路由。正式手机端新消息与回复联调待用户测试，未自动向闲鱼发送测试消息。
- 启用验证：原服务无排队或发送中任务时安全停止并重启。首次重启发现比特浏览器并发读取返回 HTTP 429；新增只读接口最多 5 次有界退避（1/2/4/8 秒，尊重可解析且不超过 30 秒的 Retry-After），浏览器打开／关闭、非 429 及网络异常不重试。保持账号昵称和 UID 校验，未切换登录来源或放宽认证。
- 最终全量测试 350 项通过；改动 Python 文件 Ruff 与 diff 空白检查通过，中文命令输出正常。再次启动后 A1/A2/A3 均 ONLINE，启动 stderr 为空，新提醒代码已加载。仅本地及启动验证通过，等待用户本人闲鱼号确认实际提醒和回复闭环。

## 连续回复与成功静默（2026-09-19）

- 正常 QUEUED / SERVER_ACCEPTED 状态仅保存在本地，不创建飞书回执；FAILED / UNKNOWN / EXPIRED 仍发一次异常提示。成功回复继续追加本地会话，客户来消息仍独立推送引用提醒，不检测或猜测详情页打开状态。
- 接收到本人消息且 parent_id 未命中客户卡片或提醒时，再精确匹配 reply_tasks.feishu_reply_message_id，复用该任务被冻结的账号 UID、客户 UID、会话 CID。无需递归追溯，不依赖上一条发送结果，不重试上一条任务。root_id 单独存在仍不能发送。
- 相同源时间的回复按 SQLite rowid（入队顺序）排队，避免随机 task_id 改变连续回复顺序。每账号发送最小间隔为 5 秒（每分钟最多 12 条），10 分钟过期保持不变。
- 重启时将旧版排队中的成功／排队回执标为 SUPPRESSED 并保留记录，不再投递；不撤回已经发送的成功回执。数据库字段与版本均不变。
- 先复现再修复成功回执、连续引用缺失路由、旧回执恢复及同时间排序问题；已覆盖多层连续回复、前序任务不同状态、跨账号切换、重启、重复事件、异常仅提示一次，以及无 parent_id 时拒绝 root_id 兜底。
- 验证：全量 362 项通过，改动文件 Ruff 与 git diff --check 通过；仅有既有 SDK datetime 弃用提示。
- 运行排查：18:55 重启后 A1/A2/A3 均进入 AUTH_REQUIRED。当时同步快照的 H5 Token 过期，三个比特窗口已关闭，因此实时接口返回空列表；这不代表保存的浏览器登录状态失效。用户授权打开原窗口后，三个账号均取得有效的新 Token，UID 与原绑定一致，无需重新扫码。已修复优先使用旧快照的问题，新增实时优先及不关闭已有窗口的回归测试。未延长缓存期限、未关闭身份校验、未切换账号来源。
- 恢复验证：19:06 安全重启后 A1/A2/A3 全部 ONLINE，启动 stderr 为空；全量 364 项测试通过，改动 Python 文件 Ruff 通过。三个比特原窗口保持打开。A3 仍有既有历史会话发现达到上限提示，不能据在线状态宣称历史补拉完整；详情页连续回复与成功静默仍待用户端验收。

## 回复间隔调整（2026-09-19）

- 按用户要求，将同账号连续回复间隔从 60 秒改为 5 秒；空闲账号首条立即发送，前一条未完成时仍串行等待。
- 主进程调度与任务领取共用 5 秒常量，账号子进程从 bridge.write_rpm_per_account 同步上游发送限流为 12 次/分钟。运行配置与示例配置同步更新，重启消息桥后生效。
- 回归覆盖 4.999 秒不放行、5 秒放行、发送中不并发，以及连续每 5 秒发送跨一分钟边界。

## 5 秒间隔运行故障分析（2026-09-19）

- 19:46:42 第一条回复成功；19:46:53 第二条失败，19:47:33 再次提交同一正文仍失败。后两条任务均记录 RateLimitedError，发生于网络发送前的本地限流检查，并非闲鱼服务端拒绝。
- 根因：worker_main 设置 GOOFISH_WRITE_RPM=12 后调用 initialize_account；该函数固定覆盖为 1，Worker 构造时还会再次调用初始化。因此队列虽为 5 秒，实际发送限流仍为每分钟 1 条。
- 上轮测试将 initialize_account 替换为空操作，绕过了真实覆盖行为；365 项测试通过及账号 ONLINE 不能证明完整启动后的 5 秒连续回复已生效。此前生效结论不充分。
- 待修复：在全部账号初始化完成后应用桥接发送额度，并用覆盖环境变量的初始化模拟或真实隔离初始化验证最终限流值和连续发送行为。本轮用户要求分析，暂不修改运行代码、不自动重发失败消息。
- 高频问题记录：修改跨层配置时必须追踪全部赋值与启动顺序；测试替身应保留影响被测行为的副作用，不能仅验证设置配置的局部路径。

## 初始化覆盖限流修复（2026-09-19）

- 已将桥接发送额度设置移到 Worker 构造完成后、事件循环运行前，覆盖入口与构造期间两次初始化的默认值。
- 回归测试保留真实 Worker 构造，初始化替身复现两次限流重置为 1 的副作用。修改前失败（实际 1，预期 12），修复后通过，并验证每 5 秒调用实际限流器共 13 次跨越一分钟窗口。
- 受影响的 worker/store/supervisor 共 56 项测试通过，改动 Python 文件 Ruff 通过；这是本地模拟验证，真实客户收件仍以服务端回包和客户实际收到为准。

## 登录自启与过期快照无头刷新（2026-09-19）

- 按用户要求配置登录桌面后自动启动。计划任务注册返回 Windows 0x80070005，安装脚本对此权限错误回退到当前用户 Startup 目录，已创建 GoofishFeishuBridge.lnk；不提升权限、不覆盖已有入口。快捷方式与计划任务不同，不提供任务计划程序的失败重启。
- 自启入口保存本机比特客户端路径 D:\bitbrowser\比特浏览器.exe。客户端未运行时后台启动，等待 Local API 就绪最多三分钟，再启动消息桥；比特客户端仍需保持登录。
- Cookie 仍在账号初始化时获取，并非运行期间轮询同步。已打开窗口优先读取实时凭据且不关闭用户窗口；已关闭时检查同步快照关键字段和 H5 Token 毫秒到期时间，预留一分钟余量。
- 无效或过期快照通过原账号无头窗口恢复：Playwright CDP 连接原上下文、加载闲鱼首页、最多轮询三十次等待有效 Cookie。保持账号昵称与 UID 绑定检查，不自动扫码，不切换账号来源。
- 打开请求本身也包含在 finally 清理范围内；读取成功、页面失败、超时或打开异常均尝试关闭临时窗口，并检查窗口进程状态。关闭请求失败或进程仍在时明确报错，不能宣称已关闭。
- 验证：先用过期快照测试复现返回旧凭据，再修复至通过。Cookie 模块 23 项、全量 377 项测试通过，改动 Python Ruff 与 PowerShell 语法检查通过。真实 A1 测试仅在内存中模拟旧快照（不修改原快照），实际无头打开、加载页面、获取有效 Cookie、关闭并确认进程退出均成功；未向闲鱼客户发送测试消息。
- 参考：[Playwright CDP 接入](https://playwright.dev/python/docs/api/class-browsertype#browser-type-connect-over-cdp)。
- 生效验证：安全停止旧监听后，使用自启快捷方式的相同命令后台启动；A1/A2/A3 均恢复 ONLINE，启动 stderr 为空，比特闲鱼分组打开窗口数为 0。未实际注销或重启 Windows，登录触发仍待下次正常登录验证。

## 回复助手范围裁剪（2026-09-19）

- 按用户确认的范围移除商品、搜索、发布配套、MCP、插件、上游技能、宣传素材、示例和上游发布流程，以及仅覆盖这些功能的测试。
- 保留登录与消息诊断 CLI、全部消息桥代码、底层协议、JS 签名、浏览器刷新、会话记录、固定路由、持久化队列、探针和相关测试，作为未来 AI 接入基础。没有新增模型调用或自动回复行为。
- 同步 README、架构、CLI 帮助、入口、依赖与 CI；保留原分发包名，避免无关迁移。许可和上游历史记录继续保留。
- 执行状态：迁移后经用户再次明确确认，已在 `E:\2-AI\xianyureply` 应用精简补丁，删除清单内 63 个文件并同步其余入口、依赖及文档；保留迁移前已有的消息桥修改。主项目全量 317 项测试、Ruff、离线锁文件检查及两个模块帮助入口验证通过；测试仅有既有飞书 SDK datetime 弃用提示。
- 缓存、旧 dist 和空 goofish-feishu-bridge 目录的后续清理命令被自动审批审查以 `blocked by policy` 拒绝，未返回具体原因，这四个目标仍保留。精简预览与补丁按约定保留。
- 检测到消息桥仍在运行，本次未停止服务或同步现有虚拟环境；pyproject.toml 与 uv.lock 已精简，环境内旧依赖和旧 MCP 启动器可能仍存在，后续维护窗口再按新锁文件同步。旧 dist 包不可用于恢复当前精简版本。
- 工作教训：单条命令被工具拒绝不能推断整个文件系统禁止删除，也不能把 Git 远程关联当作原因；应报告可验证事实并提供可审查的替代交付。

## 飞书图片回复（2026-09-20）

本轮之后的新 Skill 独立实例真实验收见 [skill-validation.md](skill-validation.md)：编号 `20260920-023451` 的一条文字经真实飞书引用入口发送，独立库状态 SERVER_ACCEPTED，用户已确认买家收到该编号文字，文字真实收发闭环通过。PNG 因 Chrome 扩展本地文件访问权限受限未发送，不能沿用原项目图片测试结论。测试实例已停止，原消息桥恢复 RUNNING，A1/A2/A3 和飞书均在线；没有回滚正式库或合并测试回复队列。

- 新增单张 PNG/JPG/JPEG 图片消息回复；JPG 与 JPEG 按实际 JPEG 编码识别，上传 MIME 为 image/jpeg。程序不主动压缩、转换或落盘保存截图。
- 直接引用原卡片、客户提醒或已关联的本人回复，冻结对应账号与客户。缺少 parent_id 时拒绝，暂不启用 root_id 路由；富文本、多图合并、文件附件、语音和视频不在本次范围。
- 增加 media 模块，飞书直连鉴权和流式下载，限制 10 MiB、2500 万像素、静态单帧；闲鱼上传继承目标账号 Session、代理及 UA。上传完成后重新检查任务过期和连接，再通过原连接发送图片消息。
- SQLite schema v4 为回复任务追加 message_type/image_key，旧任务默认 text。图片和文字同队列、同去重规则；图片下载或上传失败可提示脱敏原因，已发请求超时或领取后进程退出不自动重发。
- 安装并锁定 Pillow 到项目 .venv。改动代码 Ruff 通过，全量 393 项离线测试通过，仅有已有飞书 SDK datetime 弃用提示；中文输出校验通过。
- 已按用户要求安全停止旧实例并后台重启，A1/A2/A3 均恢复 ONLINE，启动 stderr 无报错。随后用户明确反馈“测试成功了”，确认图片发送实际链路可用；未分别报告 PNG/JPG/JPEG 各格式、大小边界和重复事件的真机结果，不扩大验收结论。

### 独立实例认证排查记录（2026-09-20）

用户确认比特窗口仍正常登录。AUTH_REQUIRED 只表示消息桥认证失败，不能直接推断浏览器已退出登录；应先核对绑定窗口、实时 Cookie、IM Token 及服务端失败阶段，再决定是否需要人工登录。已暂停新实例做单次认证诊断，不重发测试消息。

只读核对结果：新旧实例的比特 API 地址和 A1 窗口绑定一致，均指向闲鱼分组的北美草原狼；比特接口返回该窗口无运行 PID，实时 Cookie 数量为 0。无头刷新未取得有效凭据，已停止诊断及新实例，等待核对用户所见窗口，未要求重新扫码、未发送测试消息。

已定位本次误报：比特接口丢失分区信息，将有效闲鱼分区 Cookie 与旧非分区 Cookie 一起返回，旧值在扁平化时覆盖新值。新增冲突时读取原运行窗口完整 Cookie 集合，并按闲鱼站点分区选取；同作用域仍冲突则拒绝。先复现失败再修复，Cookie 模块 27 项测试及 Ruff 通过；真实读取确认凭据有效、身份一致且无冲突。

本轮独立实例手动图片验收已完成：任务库文字与图片各一条 SERVER_ACCEPTED，用户确认成功，文字和单张图片闭环通过；具体图片编码未单独确认。已停止测试实例并恢复原消息桥，A1/A2/A3 与飞书均在线，心跳新鲜。各图片格式和其他发布门槛仍按验收记录分别保留，未推送发布。

## 已登录窗口 Cookie 识别补强（2026-09-20）

- 用户反馈上午窗口已登录但程序未识别。此前记录确认过分区 Cookie 被旧值覆盖；本次代码检查又发现实时接口为空时直接报错、非空包装对象绕过空值回退、过期或缺字段的实时数据直接下传，以及 CDP 只读取一次等遗漏。
- 先补回归测试，在原实现复现 10 项失败。修复后实时数据不可用或接口失败均核对原窗口，已运行窗口通过 CDP 整组读取，最多五次、间隔一秒；不导航、不新建页面、不关闭用户窗口。持续失败说明是凭据获取问题，不能据此断定用户已退出登录。读取中窗口停止则终止，UID 不符仍拒绝。
- 分区筛选统一覆盖实时接口、快照与无头读取；只接受无同名冲突且关键字段和令牌有效期满足要求的凭据，不混用不同读取来源。闭窗且接口异常时保留异常，不触发无头打开。
- Cookie、代理、账号同步、账号隔离与传输相关 80 项离线测试、全量 443 项测试及改动 Python 文件 Ruff 已通过，中文输出正常；仅有既有飞书 SDK datetime 弃用提示。已重新生成并验证根资源清单。本轮未重启常驻服务、未写真实账号凭据，离线回归不等于原窗口实际恢复认证；新逻辑在下次加载代码并读取凭据时生效。

## 后台启动控制台弹窗修复（2026-09-20）

- 用户授权重启后曾确认 A1/A2/A3 和飞书在线；随后反馈黑色窗口、飞书测试无响应。实时检查确认主进程和三个子进程均消失，锁已释放，任务数未增加；启动 stderr 为空且没有正常退出日志，无法据此确定实际退出原因。
- 隔离 Windows 生命周期测试复现原 `DETACHED_PROCESS` 启动方式产生非零控制台句柄。改为 `CREATE_NO_WINDOW | CREATE_NEW_PROCESS_GROUP` 后，真实模拟主进程及 multiprocessing spawn 子进程控制台句柄均为零，启动器返回后存活、重复启动与安全停止测试通过。保留日志重定向、实例身份校验和停止协议。
- 使用修复后的管理入口恢复正式服务，A1/A2/A3 与飞书全部在线，启动 stderr 为空；只读附加检查确认正式主进程与三个账号子进程的控制台窗口句柄均为零。启动命令退出后再次复查，服务仍 RUNNING、心跳新鲜。全量 443 项测试及改动 Python Ruff 通过；飞书用户端收发待本次重新测试，未自动重发此前测试消息。

## 客户图片转发排查（2026-09-22）

- 用户纠正：飞书显示的 `[图片]` 是文字摘要，不能把该文字提醒的 `SERVER_ACCEPTED` 认定为原图已送达。真实 A1 测试消息在正式库中以 `text_or_summary` 保存，历史消息同一源 ID 的 custom base64 负载包含 `contentType=2` 和单张 `image.pics` URL、宽高。
- 已新增摘要触发历史补拉、同源消息补齐图片投递任务、固定 CDN 下载、WebP 内存转 JPEG/PNG、飞书图片上传和引用原客户卡片发送。单张静态图片走原客户路由；失败保留文字提醒并新增明确失败通知。相关 113 项测试及 Ruff 通过。
- 真实测试图的 CDN 返回静态 WebP，内存转换后为 864×1920 的 JPEG；图片投递任务已创建。飞书上传接口返回 `99991672`，明确要求 `im:resource:upload` 或 `im:resource`，说明现有应用尚未开通图片资源权限。一次下载失败和一次权限失败的任务均保留终态记录，未自动重复发送。最后一次安全重启后 A1～A4 与飞书均在线；原图在授权前仍未成功展示。
- 用户开通飞书图片资源权限后，对同一测试图新增一次明确重试投递；飞书图片回复接口返回 `SERVER_ACCEPTED` 和消息 ID，用户确认机器人已显示原图。原两条失败记录保留，服务复查时 A1～A4 均在线。
