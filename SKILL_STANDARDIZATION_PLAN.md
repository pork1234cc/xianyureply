# 闲鱼飞书消息桥：标准 Skill 与通用 Agent 接入方案

> 布局变更：用户已明确整个仓库本身就是 Skill，当前方案以 [ROOT_SKILL_PLAN.md](ROOT_SKILL_PLAN.md) 为准。本文的内层 skills 目录和 assets/runtime 发布副本属于历史设计，不再作为当前目标。

日期：2026-09-20

代码基线：`9092ac4`，仓库 `pork1234cc/xianyureply`
状态：2026-09-20 已按用户后续指令开始并完成本地封装与离线实施；真实平台、部分宿主及远端发布门槛尚未全部完成。第 1～17 节保留原设计与当时授权快照，当前实施结果以第 18 节及 docs/skill-validation.md 为准。

## 1. 用户目标与本次边界

已确认 ✅：

- 将现有项目封装为标准 Skill，支持通用 Agent，不限定 Codex。
- 保留闲鱼与飞书的多账号消息桥、人工文字回复及 PNG/JPG/JPEG 图片回复。
- 独立 Skill 必须支持 `npx skills add` 安装，安装包自包含运行脚本、参考资料、配置模板和项目运行资源。
- 仓库只保留一个对应的 `SKILL.md`；发布前在隔离目录完成 NPX 安装、产物完整性检查和 Skill 校验。
- 当前任务只写详细本地方案，不修改代码、不安装 Skill、不调整正在运行的消息桥、不提交或发布方案。

方案采用的默认决策：

- Skill 名称拟为 `xianyu-feishu-bridge`。
- 首版覆盖 Windows 本机上的通用 Agent 使用；操作系统移植另行立项。
- Skill 提供部署和运行管理能力；实际消息收发仍由确定性后台程序执行。
- 不新增 AI 自动回复，不让 Agent 根据客户消息自行决定发送内容或选择接收客户。
- 发布包不包含账号、Cookie、飞书密钥、聊天数据库、运行日志或本机虚拟环境。

待实施时补充 ⏳：

- 实际可用的 Agent 客户端、版本及测试机器清单；不能把安装器列出某个 Agent 视作本项目已验收。
- 发布版本号、用于验收的明确测试账号和买家会话。
- 若需要在现有生产实例上迁移，先记录实例目录、启动方式、队列状态和备份位置。本次不迁移。

这些事项不阻止方案定稿和后续离线开发。缺少真实测试目标时，不向现有客户试发。

## 2. 现状依据

| 项目部分 | 当前实现 | 封装影响 |
| --- | --- | --- |
| 程序入口 | `python -m goofish_bridge` | 可以复用，管理包装层另加统一命令 |
| 生命周期 | run、stop、status；stop 写入停止请求 | 需要有界等待、进程真实性检查和结构化返回 |
| 账号 | 每账号独立进程、目录、绑定、Cookie、代理、UA | 继续保留，不能为了 Skill 简化成一个默认账号 |
| 路由 | 飞书直接引用映射到账号、客户、会话 | 保持 `parent_id` 路由，不以最近会话猜客户 |
| 持久化 | SQLite 队列、去重、发送状态，schema v4 | 工作目录必须与 Skill 安装目录隔离 |
| 图片 | 飞书下载、目标账号上传、原 IM 连接发送 | 完整纳入运行包及 Pillow 依赖 |
| 运行检查 | doctor 检查 `.venv` 位于配置根目录 | 首版让实例继续保有自己的 `.venv`，减少迁移范围 |
| 状态 | status 主要读取数据库 | 不能仅凭旧 ONLINE 数据报告进程还活着 |
| 文件锁 | `account.py` 使用 `msvcrt` | 当前为 Windows 运行实现 |
| 自启 | PowerShell、计划任务或登录启动快捷方式 | 需增加实例标识，避免多实例共用固定任务名 |
| 发布依赖 | pyproject、uv.lock、Node.js、浏览器相关依赖 | 自包含项目文件，不打包操作系统或第三方依赖环境 |

参考代码：`src/goofish_bridge/__main__.py`、`config.py`、`account.py`、`supervisor.py`、`account_worker.py`、`store.py`、`media.py`；现有设计见 `docs/architecture.md`。

已有验证：393 项离线测试通过；消息桥重启后 A1/A2/A3 恢复 ONLINE，用户已确认图片发送测试成功。没有逐格式、跨 Agent、Skill 安装及跨操作系统验收记录，不能把现有测试结果外推为这些能力均已验证。

## 3. “通用 Agent 支持”的定义

分三个层次记录兼容性，避免笼统宣称“所有 Agent 都能运行”：

1. 格式兼容：Agent 能发现或读取标准 `SKILL.md` 和相对路径资源。
2. 执行兼容：Agent 能在实际 Windows 主机上读写文件、调用 PowerShell／Python，并按宿主权限访问网络。
3. 实测兼容：在对应客户端版本下完成安装、识别、预检和生命周期测试。

| Agent 环境 | 计划支持方式 | 首版限制 |
| --- | --- | --- |
| Codex | 标准 Skill，另提供可选 `agents/openai.yaml` | 核心说明不依赖 Codex 工具名称 |
| Claude Code | 同一个 Skill，调用相同管理入口 | 不要求 `$skill-name` 语法适用于所有宿主 |
| Cursor、OpenCode | 同一 Skill，通过安装器选择目标 | 是否自动触发、是否重载由宿主决定 |
| Hermes Agent 及其他支持该格式的宿主 | 同一说明和命令契约 | 先检查实际宿主支持与 Windows 执行能力 |
| 能读文件和执行命令、但没有 Skill 发现机制的 Agent | 用户明确指向 SKILL.md 后读取 | 属于手工使用兼容，不称为原生安装兼容 |
| 无本机工具的聊天模型 | 可以理解说明 | 不能启动本机程序，不提供虚假的运行成功结果 |
| 云端容器、WSL、Linux/macOS Agent | 可阅读 Skill | 首版不原生运行；需明确可执行命令的 Windows 主机，本项目不新增远程执行服务 |

通用正文不用 `functions.exec`、`TodoWrite`、Codex app connector 等特定工具名称。描述为“读取文件”“执行命令”“向用户询问缺失信息”，让各宿主映射自己的工具。运行成功与否以脚本的结果和进程证据为准。

不要求 MCP、插件清单或模型 API Key。若未来增加这些分发形态，应复用同一命令层，不能在首版为了兼容而重新引入已裁剪的 MCP 服务。

## 4. 总体结构

```text
用户自然语言请求
        ↓
任意具备能力的 Agent 读取同一份 SKILL.md
        ↓
解析实例路径、用户意图与必要参数
        ↓
统一管理脚本：初始化／配置／检查／启停／诊断
        ↓
实例自己的 Python 环境与现有 goofish_bridge
        ↓
后台主进程 → 各闲鱼账号进程 → 飞书与闲鱼
```

Skill 是可更新的说明和软件资源包；实例是独立、持久的运行工作目录。Agent 会话退出后，后台消息桥应继续运行，但是否能脱离宿主进程管理必须实际测试。不能仅依赖命令末尾加后台符号。

## 5. 仓库与发布包布局

保留根目录源码为唯一人工维护的业务源码，在仓库增加一个可发现的 Skill 目录：

```text
xianyureply/
├── src/、tests/、docs/、pyproject.toml、uv.lock
├── scripts/
│   ├── build_skill_bundle.py          # 拟新增：生成发布副本
│   └── verify_skill_bundle.py         # 拟新增：检查资源和哈希
└── skills/
    └── xianyu-feishu-bridge/
        ├── SKILL.md                  # 仓库内唯一入口
        ├── agents/openai.yaml        # 可选宿主元数据
        ├── scripts/
        │   ├── bootstrap.ps1         # Windows 预检和实例初始化
        │   └── bridge_control.py     # 管理命令，尽量只依赖标准库
        ├── references/
        │   ├── setup.md
        │   ├── operations.md
        │   ├── troubleshooting.md
        │   └── compatibility.md
        ├── assets/runtime/
        │   ├── src/goofish_bridge/
        │   ├── src/goofish_cli/
        │   ├── scripts/              # 精选必需的运行脚本
        │   ├── pyproject.toml
        │   ├── uv.lock
        │   ├── config.example.yaml
        │   ├── .env.example
        │   ├── README.md             # 构建元数据需要
        │   ├── LICENSE
        │   └── NOTICE
        └── bundle-manifest.json
```

发布规则：

- `assets/runtime` 是由源码生成的发布副本，禁止单独手改；每次发布校验与源文件完全一致。
- Skill 安装后的脚本只引用 Skill 内资源或明确的用户实例，不引用安装目录外的仓库源码。
- 生成器在维护仓库中读取根目录源码属于构建过程；安装后运行不依赖生成器。
- 只复制白名单文件；特别包含 `goofish_cli/static/goofish_js_version_2.js`，不能因它不是 Python 文件而遗漏。
- 构建包实际需要哪些 readme、license、脚本，按打包配置核实后纳入；不复制整个工作目录。
- 禁止目录外链接、junction 或 `../` 逃逸。`SKILL.md` 不复制到 runtime 目录，避免出现多个同名入口。
- `bundle-manifest.json` 记录发布版本、来源代码提交、相对文件路径、SHA-256 和兼容范围。排除清单自身的自引用哈希；来源提交指业务源码基线，不强求等于包含生成物的最终提交。
- 发布仓库的可安装版本必须实际包含 runtime 资源；不能只上传生成器，期待 NPX 安装时自动构建。
- 保留原项目 Apache-2.0 许可、NOTICE 与署名。

这里的“自包含”指项目自身运行文件齐全。Python、Node.js、uv、浏览器和依赖包仍是明确的环境依赖；首版安装需联网，不承诺离线安装。

## 6. Skill 正文设计

拟议 frontmatter：

```yaml
---
name: xianyu-feishu-bridge
description: 部署、配置、启动、停止和排查闲鱼与飞书消息桥；用于多账号消息转发以及本人通过飞书回复文字和图片。
compatibility: 需要可在 Windows 本机执行 PowerShell 和 Python 的 Agent；运行依赖 Python、uv、Node.js、网络访问及飞书和闲鱼账号配置。
metadata:
  version: "0.1.0"
---
```

名称、描述及相对目录遵循 Agent Skills 规范；不把试验性的宿主工具权限字段作为通用前提。标准定义和字段细节以 [Agent Skills 规范](https://agentskills.io/specification) 为依据。

正文保持短而完整，包含：适用任务、定位已安装 Skill 和实例的方法、环境检查、命令入口、结果判断、业务不变量、按需参考资料。详细飞书配置和故障排查放 references，不整份复制当前长 README。

正文明确这些行为：

- “安装 Skill”只安装技能资源；“部署消息桥”才初始化实例及其环境。
- “启动消息桥”按已有配置运行，不默认创建飞书应用或另绑定账号。
- “状态”先查真实进程与心跳，再展示数据库中的业务状态。
- “图片发不出去”先检查对应任务阶段，不反复向客户发送探测消息。
- 只有一个有效实例时直接使用；多实例且无法从明确路径、项目配置或用户上下文确定时才询问目标。
- 后续是否需要用户操作登录或填写业务参数，按实际缺项说明；普通检查和已授权启停不重复索取许可。

`agents/openai.yaml` 仅提供 Codex 展示增强，其他 Agent 忽略它也能完成所有核心任务。自动选择是否发生由宿主决定，Skill 正文不能保证被所有客户端自动调用。[Codex 官方 Skill 说明](https://learn.chatgpt.com/docs/build-skills)

## 7. 独立实例布局与路径规则

默认建议使用当前用户本地应用数据目录下的实例路径，也允许用户指定绝对路径：

```text
<instance-root>/
├── config.yaml
├── .env
├── .venv/
├── runtime/                      # 当前安装的项目代码副本
├── accounts/A*/
├── data/
├── logs/
├── backups/<时间或版本>/
└── instance.json                 # 非敏感管理元数据
```

候选默认值：`%LOCALAPPDATA%/XianyuFeishuBridge/instances/default`。这只是实例默认位置，不是 Skill 的安装位置。

首版仅默认一个实例，多账号仍由该实例管理。目录命名和元数据预留实例 ID，用于避免多个 Agent 控制同一实例时竞态；不自动建立多个实例同时登录相同闲鱼账号。

路径与环境约束：

- Skill 根目录由脚本自身文件位置解析，实例目录由显式参数或登记记录解析，不依赖 Agent 当前工作目录。
- 为兼容 `Config.root` 与 doctor，实例配置放根目录，虚拟环境保持 `<instance-root>/.venv`；运行代码安装自 `runtime/`。
- 所有子进程在同次调用设置 VIRTUAL_ENV、PATH 和 UTF-8，并调用该实例解释器的绝对路径。
- 现有项目已有有效 `.venv` 时优先复用。新部署是明确的初始化操作，检查无现成环境后说明将创建实例专用环境；不能在普通诊断中擅自创建环境或安装到全局 Python。
- 注册实例前校验目录与元数据；没有已知实例不能默默选择任意含 config.yaml 的目录。
- 配置路径、解释器路径和实例 ID写入非敏感登记信息；不登记明文凭据。
- 更新、卸载 Skill 不触及实例目录；卸载 Skill 后已经运行的服务也不会自动停止，需要用户明确管理实例。

## 8. 通用管理入口与返回契约

拟新增统一入口：`scripts/bridge_control.py`。以下子命令均为设计目标，不是当前现有 CLI：

| 命令 | 作用 | 预期副作用 |
| --- | --- | --- |
| inspect | 查看操作系统、工具版本、候选实例和环境 | 只读；不加载或打印密钥 |
| init --instance PATH | 部署包内运行资源并初始化环境与模板 | 新建已选实例，不覆盖现有配置 |
| doctor --instance PATH | 检查依赖、配置、绑定、锁和状态证据 | 默认只读，不迁移数据库、不自动刷新凭据 |
| start --instance PATH | 后台启动已配置实例并等待就绪 | 启动进程，可能读取比特凭据并恢复登录状态 |
| stop --instance PATH | 请求安全停止并核对退出 | 停止目标实例；不删除账号和队列 |
| restart --instance PATH | 停止成功后再启动 | 复用安全启停逻辑 |
| status --instance PATH | 结构化展示进程、心跳、账号和队列 | 只读 |
| diagnose --instance PATH | 收集脱敏故障摘要 | 不默认导出聊天正文和原始协议包 |
| upgrade --instance PATH | 检查版本、备份、更新和验证 | 改动运行代码／环境，可能升级数据库 |
| install-startup --instance PATH | 注册当前用户登录启动 | 仅在用户要求自启时执行 |

账号登录、比特同步和飞书绑定优先复用当前 CLI，包装层负责传递绝对配置路径以及按宿主能力安排交互。不能让 AI 自行代填当前 login/bind 的身份确认。

拟议机器输出：stdout 一个 JSON 对象，stderr 为脱敏诊断；需要交互时单独标注模式，不把人类输入提示混入 JSON。

```json
{
  "schema_version": 1,
  "operation": "status",
  "ok": true,
  "code": "RUNNING",
  "instance_id": "default",
  "process": {"alive": true, "heartbeat_fresh": true},
  "accounts": [{"key": "A1", "state": "ONLINE"}],
  "queue": {"queued": 0, "unknown": 0},
  "warnings": [],
  "next_action": null
}
```

`ok` 表示管理操作成功，不把“进程启动”误写成“所有买家收到消息”。保留 PARTIAL_READY、LOGIN_REQUIRED、CONFIG_REQUIRED、LOCKED、UNSUPPORTED_PLATFORM、TIMEOUT 等稳定代码。队列数量缺证据时用 null／unknown，不填 0 冒充已查到。

拟定包装层退出码：0 完成、2 配置或环境不完整、3 平台或能力不支持、4 实例锁冲突、5 等待超时、1 其他错误。对现有 CLI 的返回值做明确映射，不假定其含义与新契约一致。

## 9. 配置与首次接入

首次流程按阶段执行，可中断后继续：

1. inspect：确认 Windows、可执行命令、Python/uv/Node.js，以及浏览器接入方式。
2. init：部署资源、验证 manifest、安装锁定依赖，生成缺失模板。
3. configure：选择比特来源或手工登录，填写飞书 App ID、Secret、本人 OPEN_ID。
4. account setup：发现并绑定本人账号，或者指导扫码；保持 UID 固定校验。
5. feishu binding：指导本人给机器人发绑定消息，核对绑定结果。
6. doctor：完成本地检查，明确哪些项目还需要联网或真实消息验证。
7. start：后台启动并等待真实状态。
8. 由用户指定的测试会话验证文字和图片；记录实际结果，不向随机客户发送。

模板需去除现有示例中的个人展示名，改为通用占位值。新比特部署建议 `accounts: []`，按绑定结果生成；不预置三个真实账号。已有配置只补缺失字段，不整份覆盖。

密钥优先由用户在本地配置文件或宿主安全输入方式填写。若只能通过聊天输入，解释所需字段及保存位置，不要求用户把 Cookie、Secret 或完整配置贴到公开渠道；日志只显示配置是否存在。

没有终端交互能力时，输出用户可执行的明确登录/绑定命令并等待完成。不去除现有人工身份核对以换取无人值守。

## 10. 后台运行、并发和状态可靠性

现有数据库 ONLINE 可能在异常退出后滞后；新管理入口必须结合进程存活、进程启动时间、实例归属、锁和新鲜心跳判断。PID 复用时不能把无关进程当作消息桥。

建议由主进程写独立、原子更新的运行状态文件，包含 instance_id、启动时间、PID、心跳时间和程序版本；不得包含凭据。阈值以项目配置和测试结果确定，等待均有截止时间。

- start 已在运行时返回 ALREADY_RUNNING，不再启动第二套。
- stop 只操作确认属于该实例的进程，等待主进程及账号进程退出；超时先报告，不自动强杀。
- restart 只有在停止验证成功后才能继续。
- 多个 Agent 同时请求生命周期操作时，管理层锁和程序实例锁共同防止并发启动、更新。
- 飞书与闲鱼消息仍由唯一既有连接路径收发，管理 Agent 不另开正式账号发送连接。
- 后台启动隐藏窗口并重定向输出，核对退出码与日志；不能把 Start-Process 成功等同于业务就绪。
- 首选当前用户可用的独立后台进程方式；若宿主关闭会话时会回收子进程，需测试计划任务等宿主外生命周期方案。不能暗中设置永久登录自启来补救。
- 多实例自启任务名必须包含实例标识；冲突时报错，不覆盖已有任务。

## 11. 升级、回退与数据保护

安装器更新 Skill 资源与升级运行实例是两项独立操作。前者不改变正在运行的代码，后者通过 upgrade 命令受控完成。

升级顺序：

1. 校验版本、manifest、支持的 Python 和数据库版本，确认目标实例。
2. 准备新运行资源及依赖验证；准备失败不影响正在运行的旧实例。
3. 安全停止实例并确认所有写入退出。
4. 使用 SQLite 一致性备份方式保存数据库，另备份配置和账号必要文件，记录版本及哈希；不能运行中只复制 `.sqlite` 而遗漏 WAL 状态。
5. 安装新代码及环境，执行有版本门槛的数据库升级。保留旧版本与备份，不自动删除。
6. 预检和启动验证通过后标记升级完成。

回退分两类：

- 数据格式兼容且没有新的外部消息副作用时，可以切换回旧代码／环境。
- 数据库已变更或新版已经发出消息时，不自动恢复旧数据库：恢复旧队列可能造成重复发送。先保留新状态并评估兼容性和消息核对方案，必要的破坏性恢复须明确说明影响并取得确认。

卸载仅移除已明确选择的 Skill 或管理入口。删除实例、账号、数据库、备份、启动项等另列具体目标与后果；不提供默认“一键清空”步骤。当前生产项目可先作为 existing 实例登记，不能为演示迁移复制凭据后同时运行新旧实例。

## 12. NPX 安装与发布验证

目标发布入口示例（Skill 发布后才可用）：

```powershell
npx skills add https://github.com/pork1234cc/xianyureply --skill xianyu-feishu-bridge
```

开发验收在独立目录使用本地来源；指定 Agent 和复制安装模式，避免污染当前正式 Agent 的全局技能目录：

```powershell
# 在隔离工作目录执行，SOURCE 替换为待验收的仓库或完整 Skill 来源。
npx skills add SOURCE --skill xianyu-feishu-bridge --agent codex --copy
npx skills add SOURCE --skill xianyu-feishu-bridge --agent claude-code --copy
```

上述参数依据 [skills CLI 源码仓库说明](https://github.com/vercel-labs/skills)；实施时记录实际 CLI 版本并核对帮助。发布验收固定版本，用户快速安装示例可保持简洁。私有仓库需要用户已有 Git 访问权限，不把认证 Token 写进安装命令或文档。

安装验证必须针对安装产物本身：

- 列出并确认只发现目标 Skill；检查只有一个 SKILL.md 入口。
- 对照 manifest 验证运行源码、静态 JS、锁文件、配置模板、LICENSE 和 NOTICE。
- 确认路径不回指开发仓库；在源仓库不可访问的隔离环境执行预检、部署与 mock 运行验证。
- 至少验证复制模式；支持符号链接的测试机另验链接模式，不能只凭源目录运行成功判定安装完成。
- 检查 npm 安装没有自动启动服务、读取账号或发送消息；Skill 安装成功与实例部署成功分开报告。
- 用标准 Skill 校验器验证 frontmatter 和资源引用；Codex 元数据校验作为附加项，不是通用格式的唯一判定依据。
- 生成发布资源后检查 Git 差异：无真实配置、Cookie、数据库、日志、浏览器目录、虚拟环境及第二个 SKILL.md。

## 13. 验收矩阵

| 类别 | 必测场景 | 通过标准 |
| --- | --- | --- |
| 格式 | 名称、description、相对资源引用 | 标准校验通过，外部路径引用为零 |
| NPX | 隔离目录安装、复制模式、源目录不可访问 | 安装产物独立完成预检和部署 |
| Agent | Codex、至少另一款本机 Agent | 各自发现／读取技能并调用同一命令入口 |
| 能力边界 | 不支持 Windows 执行的宿主 | 明确报告限制，不声称已运行 |
| 环境 | 缺 Python／uv／Node.js、损坏环境、多个环境 | 给出准确缺项，不误用全局或其他项目环境 |
| 路径 | 空格、中文、任意 cwd、非默认磁盘 | 命令可靠定位目标，不串实例 |
| 初始化 | 空实例、已有模板、已有真实配置 | 可重复执行，不覆盖用户数据 |
| 进程 | 双 start、并发 Agent、旧 PID、陈旧 ONLINE | 不重复启动，不误报在线或误停进程 |
| 退出 | 安全停止、停止超时、宿主会话关闭 | 正确报告，后台存活行为有实际记录 |
| 账号 | 同 UID 去重、账号不匹配、代理不可用 | 沿用既有隔离和暂停行为 |
| 消息 | 文字、PNG、JPG、JPEG、错误引用、重复事件 | 正确客户收到，未知结果不自动重发 |
| 图片异常 | 超限、损坏、下载权限不足、上传失败 | 不发送无效内容，错误原因脱敏 |
| 升级 | 缺资源、依赖失败、数据库升级、重复执行 | 有备份和版本证据，失败不覆盖有效实例 |
| 卸载 | 移除 Skill | 实例数据与运行服务不被隐式删除 |
| 隐私 | 构建产物、JSON 输出、日志 | 无 Cookie、Secret、认证 URL 或完整聊天导出 |

真实平台验收必须在明确的测试会话完成。各格式是否测试、使用的 Agent/OS/依赖版本、输出结果均单独记录；未知项写“未测”。人工测试成功不替代程序回归，离线 mock 通过也不替代买家实际收件。

## 14. 分阶段实施与完成条件

### 阶段 A：分发规格与基线

- 固定名称、支持范围、命令契约和实例布局。
- 列出运行包文件白名单，记录现有测试基线和生产运行方式。
- 完成条件：方案与现有代码逐项对应，无把拟新增能力写成现成命令的问题。

### 阶段 B：自包含资源包

- 编写 SKILL.md、按需 references、可选元数据和 manifest。
- 实现构建／验证脚本，生成 runtime 副本，补齐第三方署名。
- 完成条件：目录外运行依赖检查通过，发布副本与源文件可校验一致。

### 阶段 C：实例初始化与结构化管理

- 实现环境识别、初始化、配置模板、统一结果输出和目标实例定位。
- 保留原 CLI，增量扩展进程证据与只读状态检查。
- 完成条件：可重复部署隔离实例，中文路径正确，配置和虚拟环境不串用。

### 阶段 D：启停、升级和故障恢复

- 完成后台启动、实例锁、进程验证、心跳、超时和升级备份。
- 完成条件：并发启动、状态滞后、升级失败和宿主会话结束测试通过。

### 阶段 E：跨 Agent、NPX 与真实闭环

- 从实际安装产物，在 Codex 和另一款可用 Agent 完成相同任务。
- 在授权测试会话验证文字和图片；新增版本对现有回复业务做回归。
- 完成条件：验收矩阵中所有发布必需项有证据，未覆盖的宿主明确标注未测试。

### 阶段 F：发布交付

- 更新 README 的安装入口、版本及兼容矩阵，记录包来源提交、哈希和测试版本。
- 发布前再次执行 NPX 安装、完整性与标准校验，随后按用户实际发布授权提交／推送。
- 完成条件：远端可安装、安装后脱离开发仓库运行、生产实例数据保持完整。

每阶段按模块逐次 lint 和受影响测试；Bug 修复先补复现测试。涉及超过三个文件的阶段继续拆分为小任务。只有完整阶段需要时才做 checkpoint 提交，不混入其他任务改动。

## 15. 预计改动位置

| 路径 | 预期改动 | 说明 |
| --- | --- | --- |
| `skills/xianyu-feishu-bridge/` | 新增 | 唯一 Skill 及完整发布资源 |
| `scripts/build_skill_bundle.py`、`verify_skill_bundle.py` | 新增 | 开发时生成和校验，不作为安装后外部依赖 |
| `src/goofish_bridge/__main__.py` | 小范围调整 | 结构化运行检查／必要的状态契约，保留原入口 |
| `src/goofish_bridge/supervisor.py` | 小范围调整 | 实例心跳及退出状态证据 |
| `scripts/start-bridge.ps1`、`install-startup.ps1` | 参数化 | 实例路径、实例任务名、后台输出 |
| `config.example.yaml`、`.env.example` | 模板核对 | 使用通用占位，不能修改本机真实配置 |
| `tests/` | 新增安装、路径、管理、产物验证 | 继续运行已有消息收发测试 |
| `docs/architecture.md`、README、CHANGELOG | 同步 | 写清 Skill 安装和实例运行区别 |
| `.github/workflows/ci.yml` | 按必要性扩展 | 产物一致性与 Windows 生命周期测试 |

预计不改客户路由、消息格式、限流公式、业务队列状态含义。若封装发现这些模块存在 Bug，应先记录证据再最小修复，不能借封装重写业务。

## 16. 主要风险与处理

| 风险 | 处理 |
| --- | --- |
| 同时维护根源码和包内副本产生漂移 | 生成器统一产出、manifest 和 CI 一致性检查 |
| 安装器更新覆盖运行文件 | 程序部署到独立实例，Skill 更新不热替换服务 |
| 多个 Agent 各自启动相同账号 | 默认共享已登记实例、身份校验及生命周期锁 |
| 宿主回收后台进程 | 实测会话结束行为，必要时采用明确的 OS 级运行方式 |
| 只看数据库误报 ONLINE | 进程归属、启动时间和心跳共同判断 |
| 数据库回退引起重复消息 | 不自动恢复已经产生外部副作用的旧队列 |
| 登录需要人工但 Agent 没有交互终端 | 提供本机手工步骤，等待真实完成 |
| 安装器、Agent 或平台接口变动 | 记录版本、发布前重新核验，分开记录格式和实测兼容 |
| 声称通用却依赖 Codex 私有能力 | 正文与管理协议使用普通文件和命令，不依赖宿主专有工具 |

## 17. 资料与证据索引

外部资料核对日期：2026-09-20。格式与安装事实来自下列一手资料，目录布局、管理协议、发布过程和验收设计为本项目方案，不是外部标准强制要求。

- [Agent Skills 格式规范](https://agentskills.io/specification)：元数据、资源目录及相对引用规范。
- [skills CLI 官方仓库](https://github.com/vercel-labs/skills)：NPX 安装入口、目标 Agent 选择和复制／链接模式。支持列表不等于本项目实测列表。
- [OpenAI 官方 Skill 文档](https://learn.chatgpt.com/docs/build-skills)：Codex 使用方式和可选宿主元数据。
- 本地已读取 `skill-creator` 指南：用于保持正文精简、自包含、按需加载和行为验证；方案产物运行时不依赖该本地指南路径。
- 项目 `docs/architecture.md`、`docs/bridge-development.md`：当前实现与已验证事实。

## 18. 本次交付状态

2026-09-20 用户明确“根据文档执行”，取代第 1 节原来的仅写方案边界。已执行本地开发和隔离验收；后续按用户授权临时切换独立实例真实测试，再恢复正式实例。

- 阶段 A ✅：重新核对基线 9092ac4、原有 393 项测试和正式运行进程。
- 阶段 B ✅：生成唯一 Skill、四份参考资料、完整运行包和 66 文件哈希清单；标准 skills-ref 校验及源码一致性检查通过。
- 阶段 C ✅：十个管理命令、结构化 JSON、实例登记、专用环境、模板保护、中文空格路径及只读诊断已实现并验证。
- 阶段 D：本地代码与测试 ✅；真实 Windows mock 双 start/stop、安装产物跨命令存活、SQLite WAL 备份、实际隔离升级及失败恢复通过。真正关闭宿主应用、注销登录自启仍待验证 ⏳。
- 阶段 E：NPX 1.7.0 双 Agent 复制安装、当前 Codex 会话和 Claude Code 2.1.274 的实际读取/调用 ✅。独立 Codex CLI 0.155.1 被本机沙箱目录权限错误阻止；其他宿主、OS 强隔离、真实客户格式逐项验收未完成 ⏳。
- 阶段 F：README、架构、开发记录、CHANGELOG、Windows CI 和验收记录已更新 ✅；尚未提交、推送或远端发布 ⏳。

最终全量离线测试 410 项通过，Ruff 和发布资源校验通过，仅有既有 SDK 弃用提示。Windows PowerShell 编码和临时目录自启快捷方式验证通过；未修改真实登录启动配置。后续真实验收使用用户指定 A1 和测试买家，以“Skill 验收起点”精确锁定会话，通过飞书引用实际发送一条文字（编号 `20260920-023451`），独立实例记录 SERVER_ACCEPTED，用户已确认买家收到该编号文字，文字真实收发闭环通过。PNG 上传被 Chrome 扩展本地文件访问权限阻止，尚未发送；JPG/JPEG 仍未测试。测试实例已停止，正式实例恢复 RUNNING，A1/A2/A3 与飞书均在线。未迁移、回滚或复制测试回复队列到生产，未删除账号、数据库或备份。临时实例现含私有配置，保留本地且不得发布。

关键实施选择：不添加可选 openai.yaml；原 PowerShell 启动脚本保留供旧项目使用，Skill 自启使用实例内的独立管理入口和含实例 ID 的快捷方式。首版 upgrade 只接受 schema 4 或尚未建库，不自动处理其他版本数据库。旧项目不自动登记或迁移，防止无意创建第二套在线账号。

详细版本、错误证据和剩余验收门槛见 [docs/skill-validation.md](docs/skill-validation.md)。这些未测项目不能按本地 mock 通过推定完成，远端安装也不能在实际推送前宣称可用。

最新手动验收窗口：分区 Cookie 修复已通过 413 项回归并部署到新 Skill 独立实例；实际进程 RUNNING、心跳新鲜、A1 ONLINE、飞书在线。当前保留新实例供用户手动测试原指定买家图片，原三账号实例未启动，A2/A3 暂停。图片发送和买家收件仍待本轮确认。

本轮独立实例手动图片验收已完成：任务库文字与图片各一条 SERVER_ACCEPTED，用户确认成功，文字和单张图片闭环通过；具体图片编码未单独确认。已停止测试实例并恢复原消息桥，A1/A2/A3 与飞书均在线，心跳新鲜。各图片格式和其他发布门槛仍按验收记录分别保留，未推送发布。
